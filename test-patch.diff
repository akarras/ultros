--- ultros-frontend/ultros-app/src/components/sale_history_table.rs
+++ ultros-frontend/ultros-app/src/components/sale_history_table.rs
@@ -453,7 +453,7 @@
         let d4 = datetime(2020, 1, 4);
         let range = d2..=d3;

-        let result = find_date_range(range, &sales);
+        let result = find_date_range(&range, &sales);
         assert!(result.is_some());
         let result = result.unwrap();
         assert_eq!(result.len(), 2);
@@ -465,7 +465,7 @@
         let sales: Vec<SaleHistory> = vec![];
         let range = datetime(2020, 1, 2)..=datetime(2020, 1, 3);

-        assert!(find_date_range(range, &sales).is_none());
+        assert!(find_date_range(&range, &sales).is_none());
     }

     #[test]
@@ -477,7 +477,7 @@
         ];
         let range = datetime(2020, 1, 5)..=datetime(2020, 1, 6);

-        assert!(find_date_range(range, &sales).is_none());
+        assert!(find_date_range(&range, &sales).is_none());
     }
 }
