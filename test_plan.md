1. Add `type="button"` to the search clear button in `ultros-frontend/ultros-app/src/components/search_box.rs`.
2. Add `type="button"` to the job example buttons in `ultros-frontend/ultros-app/src/components/search_box.rs`.
3. Add `type="button"` to the AddRecipeToList button in `ultros-frontend/ultros-app/src/components/add_recipe_to_list.rs`.
4. Ensure `aria-label` attributes on icon-only buttons remain well-defined.
