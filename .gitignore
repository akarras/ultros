target/
# Per-machine sparse checkouts of the upstream game-data sources (game-data-pack)
.game-data-cache/
.env
# Agent-local MCP config — may contain tokens / machine-specific paths
.mcp.json
.DS_Store
executables/
integration/node_modules/
integration/artifacts/
context.md
docker-compose.yml
analyzer-data/*.bin.gz

# Generated by the ultros-charts design example
sample-chart.svg

# Superpowers brainstorming mockups
.superpowers/

# Local Claude Code workspace: dev-server launch config, per-user settings,
# and agent worktrees. Machine-specific, not shared config.
.claude/

# Merge/patch leftovers. Jules has committed these into a PR more than once
# (#1197 shipped a .orig, a .rej and three loose .patch/.diff files).
*.orig
*.rej
