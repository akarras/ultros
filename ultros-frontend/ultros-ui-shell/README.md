# ultros-ui-shell

Extracted modules: account_menu, ad, character_switcher, home_world_menu, language_picker, mobile_bar, profile_display, region_menu, search_box, search_overlay, theme_picker, update_banner.

Enable `ssr` or `hydrate` to match the application. Module and context types are re-exported by `ultros-app`; do not provide duplicate contexts.
