## 2024-05-30 - [Leptos UI Value Testing] Learning: Testing frontend UI formatters and logic filters avoids silent UX failures. Action: Always add comprehensive tests for analysis formatters in Leptos/Axum apps since they heavily influence users' view on data.
## 2024-05-30 - [Robust Zero IQR Stats Analysis] Learning: When computing averages on recent sales logic and analyzing market values, the Interquartile Range (IQR) method encounters edge cases like an IQR of 0 if items map to the exact same price. Action: Ensure outlier calculation functions include specific validation for a zero-IQR path to prevent crashes or silent filtering errors.
## 2024-06-01 - [take_while bug in pricing] Learning: When computing cheapest prices by taking elements from a list up to a target quantity, simply using `current_quantity <= quantity_needed` in a `take_while` can drop the last listing needed to fulfill an order if that listing pushes the quantity over the target. Action: When summing quantities using iterator methods like `take_while`, use the quantity accumulated *before* adding the current item's quantity for the condition check to ensure the boundary item is included.
## 2024-06-01 - [take_while fix in list_summary.rs] Learning: Found the same take_while dropping boundary elements in list_summary.rs as was found in recipepricecheck previously. Action: Applied the same logic (storing the condition variable before incrementing) to list_summary.rs. The components test confirms boundary inclusion correctly.
## 2026-06-15 - [group_sales_by_level explicit levels] Learning: When testing world-hierarchy grouping, explicit levels correctly group elements bypassing auto-grouping logic. Action: Always test explicit grouping behavior to ensure hierarchical constraints are met.
## 2026-06-16 - [Virtual Scroller Binary Search] Learning: When computing virtual scroll positions with negative deltas (i.e. elements shrinking below their assumed height), the cumulative height prefix sum array isn't strictly growing monotonically relative to the assumed layout sizes, but the binary search correctly accounts for it by finding the first index where `base_height + delta >= effective_scroll`. Action: Virtual scroller logic uses `Fenwick` tree. Ensure we verify negative deltas don't panic or misalign logic, and write tests that verify the exact `find_first_gte` search logic applied inside the virtual_scroller macro loop.
## 2025-02-23 - [Leptos UI Component Styling Tests] Learning: Leptos UI components often map internal state directly to classes or assets, making testing difficult due to reactive dependencies. Action: Extract mappings (like enum variants to CSS classes/icons) into pure testable functions so they can be thoroughly unit tested without mocking leptos reactive context.
## 2025-02-23 - [Leptos UI Number Formatters Tests] Learning: Pure numerical formatting functions embedded in Leptos components (like `compact_number` inside `market_pulse.rs`) determine exactly what string representation is displayed. Leaving these un-tested can lead to silent visual bugs such as poor rounding or broken truncation boundaries. Action: Always add explicit test modules to ensure numerical formatter abbreviations logic works correctly across thresholds (e.g. 10k, 1M, 1B).
